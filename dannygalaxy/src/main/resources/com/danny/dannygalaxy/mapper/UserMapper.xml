<?xml version="1.0" encoding="UTF-8"?>

<!-- 문서 유형 : mapper -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.danny.dannygalaxy.mapper.UserMapper">
	
	<!--회원 전체 목록 조회  -->
	<select id="selectUserList" resultType="com.danny.dannygalaxy.domain.UserVO">
		SELECT * 
	      FROM schatz.users_table
	</select>
	
	<!--회원 가입  -->
	<insert id="insertUser" parameterType="com.danny.dannygalaxy.domain.UserVO">
		<selectKey keyProperty="user_idx" order="BEFORE">
			SELECT schatz.user_seq.nextval from dual
		</selectKey>
		INSERT INTO schatz.users_table
		     VALUES(#{user_seq},#{user_id},#{user_name},#{user_password},#{user_email},DEFAULT)
	</insert>

	<!--회원 탈최  -->
	<delete id="deleteUser" parameterType="com.danny.dannygalaxy.domain.UserVO">
		DELETE schatz.users_table
		 WHERE user_idx = #{user_idx}
	</delete>

   <!--로그인  -->
	<select id="selectLoginUserInfo" parameterType="com.danny.dannygalaxy.domain.UserVO" 
									resultType="com.danny.dannygalaxy.domain.UserVO">
			SELECT user_idx,user_name
             FROM schatz.users_table
            WHERE user_id = #{user_id} AND user_password = #{user_password}
									
									
	</select>

  <!-- 아이디 중복체크  -->		
	<select id="checkUserIdExist" parameterType="String" resultType="String">
			SELECT (CASE when count(*)=1 then 'unusable' ELSE 'usable' END)as RESULT
			 FROM schatz.users_table
			WHERE user_id = #{user_id}
	</select>
			


</mapper>